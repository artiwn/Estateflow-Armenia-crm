import {t} from "../i18n";
import{bankStatementLines,deals,paymentReceipts}from"../data/mock";
import{badge,compactAmd,money,statusLabel}from"../utils";
import{navigate}from"../router";
import{closeModal,formValue,openLocalizedModal,toastLocalized}from"../components/layout";
import{importBankStatement,readTabularFile}from"../services/importers";
import{demoText}from"../demo-i18n";
import {activeExchangeRate,confirmBankReceipt,convertCurrency,toAMD} from "../services/finance-control";

export function reconciliationPage(){const totalAMD=bankStatementLines.reduce((s,x)=>s+toAMD(x.amount,x.currency,x.exchangeRate),0),suggested=bankStatementLines.filter(x=>x.status==="suggested").length,unmatched=bankStatementLines.filter(x=>x.status==="unmatched").length,confirmed=bankStatementLines.filter(x=>x.status==="confirmed").length,foreign=bankStatementLines.filter(x=>x.currency!=="AMD").length;return`<section class="page"><div class="page-header"><div class="page-title"><div class="eyebrow">${t("reconciliation.workspace")}</div><h1>${t("reconciliationPage.title")}</h1><p>${t("v50.recon.subtitle")}</p></div><div class="toolbar"><button class="btn" id="backFinance">${t("reconciliationPage.backFinance")}</button><button class="btn btn-primary" id="importStatement">${t("reconciliationPage.importStatement")}</button></div></div><div class="finance-metrics finance-metrics-v50 u-mb-16"><div class="card metric-card"><div class="metric-top"><span>${t("reconciliationPage.statementTotal")}</span></div><div class="metric-value u-fs-20">${compactAmd(totalAMD)}</div><div class="metric-note">${t("v50.finance.normalized")}</div></div><div class="card metric-card"><div class="metric-top"><span>${t("reconciliationPage.autoMatch")}</span></div><div class="metric-value">${suggested}</div><div class="metric-note">${t("reconciliationPage.readyToConfirm")}</div></div><div class="card metric-card"><div class="metric-top"><span>${t("reconciliationPage.unmatched")}</span></div><div class="metric-value">${unmatched}</div><div class="metric-note">${t("reconciliationPage.manualProcessingRequired")}</div></div><div class="card metric-card positive"><div class="metric-top"><span>${t("reconciliationPage.confirmed")}</span></div><div class="metric-value">${confirmed}</div><div class="metric-note">${t("reconciliationPage.reflectedInSchedules")}</div></div><div class="card metric-card accent"><div class="metric-top"><span>${t("v50.recon.foreignCurrency")}</span></div><div class="metric-value">${foreign}</div><div class="metric-note">${t("v50.recon.foreignCurrencyNote")}</div></div></div><div class="recon-layout"><div class="card"><div class="card-pad table-toolbar"><div><strong>${t("reconciliationPage.statementDate")}</strong><div class="subtle">Ameriabank · ACBA BANK · Ardshinbank · USD/AMD</div></div><div class="toolbar"><select class="field" id="reconCurrency"><option value="">${t("v50.payments.allCurrencies")}</option><option>AMD</option><option>USD</option><option>EUR</option></select><select class="field" id="reconFilter"><option value="">${t("reconciliationPage.allRows")}</option><option value="suggested">${statusLabel("suggested")}</option><option value="unmatched">${statusLabel("unmatched")}</option><option value="confirmed">${statusLabel("confirmed")}</option></select></div></div><div class="recon-list">${bankStatementLines.map(x=>row(x)).join("")}</div></div><aside class="card card-pad recon-inspector" id="reconInspector">${inspector(bankStatementLines[0].id)}</aside></div></section>`}
function row(x:(typeof bankStatementLines)[number]){const bank=demoText(x.bank),bookingDate=demoText(x.bookingDate);return`<button class="recon-row ${x.status}" data-recon="${x.id}" data-recon-status="${x.status}" data-recon-currency="${x.currency}"><div class="recon-bank"><span class="bank-avatar mini">${bank.slice(0,2).toUpperCase()}</span><div><strong>${x.payer}</strong><span>${bank} · ${bookingDate}</span></div></div><div class="recon-purpose"><strong>${demoText(x.purpose)}</strong><span>${x.reference}</span></div><div class="recon-amount"><strong>${money(x.amount,x.currency)}</strong><small>${x.currency!=="AMD"?`≈ ${compactAmd(toAMD(x.amount,x.currency,x.exchangeRate))}`:""}</small><span class="confidence ${x.confidence>=80?"high":x.confidence>=50?"medium":"low"}">${x.status==="confirmed"?statusLabel("confirmed"):`${x.confidence}% ${t("reconciliationPage.match")}`}</span></div></button>`}
function inspector(id:string){const x=bankStatementLines.find(v=>v.id===id)!;const deal=deals.find(d=>d.id===x.matchedDealId),dealAmount=deal?convertCurrency(x.amount,x.currency,deal.currency,x.exchangeRate):undefined,receipt=x.receiptId?paymentReceipts.find(r=>r.id===x.receiptId):undefined;return`<div class="section-title"><div><h2>${t("reconciliationPage.matching")}</h2><p>${x.id}</p></div>${badge(x.status)}</div><div class="recon-amount-big">${money(x.amount,x.currency)}</div>${x.currency!=="AMD"?`<div class="fx-conversion-strip"><div><span>${t("v50.recon.rate")}</span><strong>1 ${x.currency} = ${(x.exchangeRate??activeExchangeRate(x.currency)).toFixed(2)} AMD</strong></div><div><span>${t("v50.recon.amdEquivalent")}</span><strong>${compactAmd(toAMD(x.amount,x.currency,x.exchangeRate))}</strong></div>${deal?`<div><span>${t("v50.recon.dealAmount")}</span><strong>${money(dealAmount!,deal.currency)}</strong></div>`:""}</div>`:""}<div class="info-list one"><div class="info-box"><span>${t("reconciliationPage.payer")}</span><strong>${x.payer}</strong></div><div class="info-box"><span>${t("reconciliationPage.purpose")}</span><strong>${demoText(x.purpose)}</strong></div><div class="info-box"><span>${t("reconciliationPage.reference")}</span><strong>${x.reference}</strong></div><div class="info-box"><span>${t("reconciliationPage.matchedDeal")}</span><strong>${x.matchedDealId??t("reconciliationPage.notDefined")}</strong></div>${deal?`<div class="info-box"><span>${t("v50.recon.dealCurrency")}</span><strong>${deal.currency}</strong></div>`:""}<div class="info-box"><span>${t("v50.recon.accountingStatus")}</span><strong>${t(`v50.accounting.${x.accountingStatus??'unreviewed'}`)}</strong></div></div><div class="match-score"><div><span>${t("reconciliationPage.algorithmConfidence")}</span><strong>${x.confidence}%</strong></div><div class="progress"><span style="width:${x.confidence}%"></span></div></div>${receipt?`<div class="receipt-result"><span>${t("v50.recon.receiptCreated")}</span><strong>${receipt.id}</strong><small>${t("v50.payments.allocated")}: ${money(receipt.allocatedAmount,receipt.dealCurrency)}${receipt.unallocatedAmount?` · ${t("v50.payments.advance")}: ${money(receipt.unallocatedAmount,receipt.dealCurrency)}`:""}</small></div>`:""}<div class="toolbar recon-actions"><button class="btn btn-primary" data-confirm-recon="${x.id}">${x.status==="confirmed"?t("reconciliationPage.alreadyConfirmed"):x.matchedDealId?t("reconciliationPage.confirmMatch"):t("reconciliationPage.chooseDeal")}</button>${x.status!=="confirmed"?`<button class="btn" data-manual-recon="${x.id}">${t("reconciliationPage.change")}</button>`:""}</div>${x.matchedDealId?`<button class="text-btn" data-open-deal="${x.matchedDealId}">${t("reconciliationPage.open")} ${x.matchedDealId} →</button>`:""}`}
export function bindReconciliation(){const bindInspector=()=>{document.querySelectorAll("[data-confirm-recon]").forEach(el=>el.addEventListener("click",()=>confirmLine((el as HTMLElement).dataset.confirmRecon!)));document.querySelectorAll("[data-manual-recon]").forEach(el=>el.addEventListener("click",()=>manualMatch((el as HTMLElement).dataset.manualRecon!)));document.querySelectorAll("[data-open-deal]").forEach(el=>el.addEventListener("click",()=>navigate("deal/"+(el as HTMLElement).dataset.openDeal)))};document.querySelector("#backFinance")?.addEventListener("click",()=>navigate("finance"));document.querySelector("#importStatement")?.addEventListener("click",()=>{openLocalizedModal(t("reconciliationPage.importModalTitle"),`<div class="modal-note">${t("v50.recon.importHelp")}</div><div class="form-field u-mt-14"><label>${t("reconciliationPage.statementFile")}</label><input id="bankImportFile" type="file" accept=".csv,.xlsx,.xls" required></div>`,`<button class="btn" data-modal-close>${t("reconciliationPage.cancel")}</button><button class="btn btn-accent" id="runBankImport">${t("reconciliationPage.import")}</button>`);setTimeout(()=>document.querySelector("#runBankImport")?.addEventListener("click",async()=>{const file=document.querySelector<HTMLInputElement>("#bankImportFile")?.files?.[0];if(!file){toastLocalized(t("reconciliationPage.chooseFile"));return}try{const rows=await readTabularFile(file),result=importBankStatement(rows);closeModal();toastLocalized(t("reconciliationPage.importResult",{added:result.added,skipped:result.skipped,errors:result.errors.length}));refreshReconciliation(bankStatementLines.at(-1)?.id??bankStatementLines[0].id)}catch{toastLocalized(t("reconciliationPage.importError"))}}),0)});document.querySelectorAll("[data-recon]").forEach(el=>el.addEventListener("click",()=>{const ins=document.querySelector("#reconInspector");if(ins){ins.innerHTML=inspector((el as HTMLElement).dataset.recon!);bindInspector()}}));document.querySelector("#reconFilter")?.addEventListener("change",filterRows);document.querySelector("#reconCurrency")?.addEventListener("change",filterRows);bindInspector()}
function confirmLine(id:string){const x=bankStatementLines.find(v=>v.id===id);if(!x)return;if(!x.matchedDealId){manualMatch(id);return}if(x.status==="confirmed"){toastLocalized(t("reconciliationPage.paymentAlreadyConfirmed"));return}try{const receipt=confirmBankReceipt(x);toastLocalized(receipt?.unallocatedAmount?t("v50.recon.confirmedWithAdvance",{amount:money(receipt.unallocatedAmount,receipt.dealCurrency)}):t("reconciliationPage.paymentConfirmedAllocated"));refreshReconciliation(id)}catch{toastLocalized(t("v50.recon.confirmFailed"))}}
function manualMatch(id:string){const x=bankStatementLines.find(v=>v.id===id);if(!x)return;if(x.status==="confirmed"){toastLocalized(t("reconciliationPage.cannotReassignConfirmed"));return}openLocalizedModal(t("reconciliationPage.manualMatchTitle"),`<form id="manualReconForm" class="form-grid"><div class="form-field full"><label>${t("reconciliationPage.receipt")}</label><input disabled value="${x.payer} · ${money(x.amount,x.currency)}"></div><div class="form-field full"><label>${t("reconciliationPage.deal")}</label><select name="dealId" required><option value="">${t("reconciliationPage.chooseDeal")}</option>${deals.map(d=>`<option value="${d.id}" ${x.matchedDealId===d.id?"selected":""}>${d.id} · ${d.clientName} · ${d.propertyLabel} · ${d.currency}</option>`).join("")}</select></div><div class="form-field full"><label>${t("reconciliationPage.accountantComment")}</label><textarea name="note">${t("reconciliationPage.manualReviewNote")}</textarea></div></form>`,`<button class="btn" data-modal-close>${t("reconciliationPage.cancel")}</button><button class="btn btn-accent" id="saveManualRecon">${t("reconciliationPage.link")}</button>`);setTimeout(()=>document.querySelector("#saveManualRecon")?.addEventListener("click",()=>{const f=document.querySelector<HTMLFormElement>("#manualReconForm")!;if(!f.reportValidity())return;x.matchedDealId=formValue(f,"dealId");x.confidence=100;x.status="suggested";closeModal();toastLocalized(t("reconciliationPage.manualDealSelected"));refreshReconciliation(id)}),0)}
function refreshReconciliation(selected:string){navigate("payments");setTimeout(()=>{navigate("reconciliation");setTimeout(()=>{const el=document.querySelector<HTMLElement>(`[data-recon="${selected}"]`);el?.click()},30)},20)}
function filterRows(){const st=document.querySelector<HTMLSelectElement>("#reconFilter")?.value??"",currency=document.querySelector<HTMLSelectElement>("#reconCurrency")?.value??"";document.querySelectorAll<HTMLElement>("[data-recon]").forEach(r=>r.style.display=(!st||r.dataset.reconStatus===st)&&(!currency||r.dataset.reconCurrency===currency)?"":"none")}
